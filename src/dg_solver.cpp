#include "dg_solver.h"
template<typename T>
inline void DGSolver<T>::add_constraint(ConstraintType s, std::vector<int> t)
{
}
template<typename T>
void DGSolver<T>::solve(const Eigen::Matrix<T, -1, -1, Eigen::RowMajor>& coords)
{
}
template<typename T>
void DGSolver<T>::add_deformation_gradient(DeformationGradient<T> dg)
{
}